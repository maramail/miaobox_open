using System.Collections.Generic;
public class stat_catRow
{
    public int id;
    public string name;
    public int lv;
    public int evo;
    public int atk;
    public int cooldown;
    public int weight;
    public int paymoney;
    public int hireexp;
    public int ownexp;
    public int exceedlimit;
    public string about;
    public string voice;
    public string illustra;
    public int weapon_on;
    public int addon_sideleft_on;
    public int addon_sideright_on;
    public int addon_back_on;
    public int attack_aniid;
    public int skill_active_id;
    public int skill_active_aniid;
    public int skill_always_id;
    public int skill_always_aniid;
    public int skill_passive_id;
    public int skill_passive_aniid;
}
/// <summary>
/// Auto Generated By ZJRTool, Do Not Modify
/// </summary>
public class stat_cat : DatabaseSerializer
{
    private static stat_cat mInstance;
    public List<stat_catRow> rowList = new List<stat_catRow>();
    public static stat_cat GetInstance()
    {
        return mInstance ?? (mInstance = new stat_cat());
    }
    protected override void AddRow(object[] rowInfo)
    {
        stat_catRow row = new stat_catRow();
        row.id = GetInt(rowInfo[0]);
        row.name = GetString(rowInfo[1]);
        row.lv = GetInt(rowInfo[2]);
        row.evo = GetInt(rowInfo[3]);
        row.atk = GetInt(rowInfo[4]);
        row.cooldown = GetInt(rowInfo[5]);
        row.weight = GetInt(rowInfo[6]);
        row.paymoney = GetInt(rowInfo[7]);
        row.hireexp = GetInt(rowInfo[8]);
        row.ownexp = GetInt(rowInfo[9]);
        row.exceedlimit = GetInt(rowInfo[10]);
        row.about = GetString(rowInfo[11]);
        row.voice = GetString(rowInfo[12]);
        row.illustra = GetString(rowInfo[13]);
        row.weapon_on = GetInt(rowInfo[14]);
        row.addon_sideleft_on = GetInt(rowInfo[15]);
        row.addon_sideright_on = GetInt(rowInfo[16]);
        row.addon_back_on = GetInt(rowInfo[17]);
        row.attack_aniid = GetInt(rowInfo[18]);
        row.skill_active_id = GetInt(rowInfo[19]);
        row.skill_active_aniid = GetInt(rowInfo[20]);
        row.skill_always_id = GetInt(rowInfo[21]);
        row.skill_always_aniid = GetInt(rowInfo[22]);
        row.skill_passive_id = GetInt(rowInfo[23]);
        row.skill_passive_aniid = GetInt(rowInfo[24]);
        rowList.Add(row);
    }
}
